python-dotenv>=1.0.0
httpx>=0.27.0
pysimdjson>=5.0.0
PyMySQL>=1.1.0
pytz
# Multi-core bz2 decompression for the local dump file (speeds every ETL pass).
# Ships manylinux wheels, so no build toolchain is needed in the slim image.
# The ETL falls back to single-threaded stdlib bz2 if this import ever fails.
indexed_bzip2>=1.5.0